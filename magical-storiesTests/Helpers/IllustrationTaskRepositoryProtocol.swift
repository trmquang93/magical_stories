import Foundation
@testable import magical_stories

// This file exists to re-export the protocol from the main app module
// to ensure it's available in test targets

// Note: The actual protocol is imported from magical_stories module
// We don't define it here to avoid duplicate symbol errors
